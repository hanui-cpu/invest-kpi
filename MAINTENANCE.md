# KPI 대시보드 관리 안내 (사람용)

> AI(Claude)와 함께 작업한다면 이 저장소 루트의 `CLAUDE.md`를 Claude가 자동으로 읽습니다.
> Claude에게 "이 저장소의 ○○를 고쳐줘"라고 요청하면 됩니다.

## 대원칙 — 이것만 기억하세요

**모든 수정은 GitHub 웹에서 고치고 → 초록 버튼으로 반영하면 끝. Vercel은 절대 직접 건드리지 않습니다.**

main 브랜치에 반영(Merge)되면 Vercel이 1~2분 안에 실서비스(dcamp-invest-kpi.vercel.app)에 자동 배포합니다.
Vercel에 수동으로 올리면 GitHub 코드와 실서비스가 어긋나는 사고가 재발합니다 (2026-07 실제 발생).

## 파일 지도 — 무엇을 고치려면 어떤 파일?

| 고치고 싶은 것 | 파일 | 담당 |
|---|---|---|
| 투자팀 페이지 (AI 브리핑 포함) | `index.html` | 투자팀 |
| 펀드팀 페이지 | `fund.html` | 펀드팀 |
| 의무투자 배수 시뮬레이터 | `drypowder.html` | 펀드팀 (⚠️ 아래 특이사항) |
| 두 팀 공통 (헤더·버튼 디자인·PDF/PPT 다운로드) | `index.html`과 `fund.html` **둘 다** | 상호 협의 |
| 시트 데이터 중계 서버 함수 | `api/sheet.js` | 투자팀 |
| AI 브리핑 서버 함수 | `api/briefing.js` | 투자팀 |
| 시뮬레이터 데이터 서버 함수 | `api/funds.js` | 펀드팀 |

## 수정 절차 — 브라우저 클릭만으로 (명령어 불필요)

1. **파일 열기**: github.com/hanui-cpu/invest-kpi → 수정할 파일 클릭
2. **수정**: 오른쪽 위 연필 아이콘(✏️) 클릭 → 편집 (위치 찾기는 Ctrl+F)
3. **저장**: 초록 **Commit changes...** 버튼 → 창에서
   - 수정 내용 한 줄 입력 (예: "펀드팀 목표 문구 수정")
   - **"Create a new branch ... and start a pull request" 선택** ← 안전장치, 꼭 이쪽으로
   - **Propose changes** 클릭
4. **Create pull request** 클릭
5. **Merge pull request → Confirm merge** 클릭
   (상대 팀 확인이 필요한 수정이면 머지 전에 PR 링크를 공유)
6. 1~2분 뒤 실서비스에서 확인. **이상하면 PR 페이지의 Revert 버튼 → 다시 Merge** 하면 즉시 원상복구

## 꼭 지킬 것 3가지

1. **Vercel 수동 배포 금지** — 배포는 오직 main Merge로만
2. **공통 요소는 두 파일 다** — 헤더/디자인/다운로드 기능은 index.html과 fund.html에 같은 코드가 복제되어 있음
3. **확장자 없는 `api/funds` 파일을 올리지 말 것** — 서버 함수를 가려서 시뮬레이터 실시간 연동이 죽음 (.gitignore로 막아둠)

## 시뮬레이터(drypowder.html) 특이사항

- 원본은 펀드팀 프로젝트 폴더에서 별도 관리되며, 수치·계산 로직 변경은 **독립 재계산 검증**을 거친 뒤 반영합니다 → 펀드팀(주흠)에게 요청
- 배포본과 원본의 유일한 차이는 헤더의 "← 펀드팀 KPI 대시보드" 복귀 버튼 한 줄 (주석 표시됨)

## 자주 겪는 상황

- **구글시트의 열/헤더 문구를 바꿨더니 화면이 이상함** → 파서가 헤더 문구로 표를 찾기 때문. 실제 사례: 해외 표에 "통화구분" 열이 생기자 국내 펀드 목록이 0건이 됨. 시트 구조 변경 후엔 반드시 실서비스 확인, 이상하면 담당자/Claude에게
- **파일을 컴퓨터에서 열면 "데이터 fetch 실패"** → 정상. 데이터는 Vercel 서버 함수를 통해서만 나옴. 실서비스에서 확인할 것
- **PR 미리보기(Preview) 링크에서 데이터가 안 뜸** → Vercel 환경변수가 Production에만 적용된 경우. 화면 구조만 확인하고 머지 후 실서비스에서 데이터 확인
